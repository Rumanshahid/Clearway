// Shared billing constants. The Practice plan price is also configured as an
// actual Paddle price object (NEXT_PUBLIC_PADDLE_PRICE_ID_PRACTICE) — keep
// this number in sync with whatever that Paddle price is set to.
export const PRACTICE_MONTHLY_PRICE_USD = 249;
export const PILOT_LETTERS_INCLUDED = 10;
