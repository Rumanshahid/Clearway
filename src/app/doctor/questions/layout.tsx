// Same nav bar / billing-banner / auth-guard layout as the rest of the
// staff dashboard -- see doctor/dashboard/layout.tsx for why this needs
// its own re-export rather than inheriting one automatically.
export { default } from "@/app/dashboard/layout";
